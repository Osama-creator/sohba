import 'package:flutter/material.dart';

class ChalengesTab extends StatelessWidget {
  const ChalengesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Chalenges Tab'),
      ),
    );
  }
}
